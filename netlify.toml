[build]
  publish = "."

[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404

# Nota: esta redirección solo se aplica a rutas que no existan.
# Las páginas reales (index.html, tienda.html, etc.) se sirven con normalidad.
